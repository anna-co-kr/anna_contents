# Prompt Studio v0.5 MVP PRD

## 핵심 정보

**목적**: 안나가 AI 이미지 생성(Midjourney, Nano Banana) 시 프롬프트 수렴에 소비하는 포스트당 2시간 병목을 레퍼런스 축적·페어 로그·토큰 분석으로 해소한다.
**사용자**: 안나 1인 (Instagram/YouTube 글로벌 디자인 큐레이터 채널, 단일 Supabase 계정)
**성공 기준**: 도구 사용 시 포스트 제작 시간 T0 대비 **-60%**, 만족 결과까지의 이터레이션 횟수 **-50%**. 상세 베이스라인·측정 절차·단계별 타겟은 `ROADMAP.md` Phase 0 Task 000(T0) 및 Phase 3 Task 022-024(T1·T2·T3 실측) 참조.

---

## 사용자 여정

```
1. 로그인 페이지
   ↓ Supabase Auth 인증 성공 (단일 계정)

2. 레퍼런스 라이브러리 페이지 (메인 허브)
   ↓ 진입 후 선택

   [URL/이미지 드롭] → Claude Vision 토큰 분해 → 레퍼런스 카드 생성
                       ↓
                    [좋아요 점수·태그·프롬프트 스니펫 입력]
                       ↓
                    [copy prompt] 버튼 → 클립보드 → MJ에 붙여넣기

   [레퍼런스 카드 클릭] → 레퍼런스 상세 페이지

3. 레퍼런스 상세 페이지
   ↓ 프롬프트 스니펫 확인 후

4. 프롬프트 페어 로그 페이지
   ↓ MJ에서 실행한 프롬프트 + 결과 이미지 pair 저장

   [만족 결과 마킹] → 성공 페어로 기록
   [페어 목록 클릭] → 토큰 diff 뷰 (V1.5)

5. 토큰 diff 페이지 (V1.5)
   ↓ 성공/실패 프롬프트 토큰 차이 확인

6. 유사 레퍼런스 검색 페이지 (V1.5)
   ↓ pgvector 유사도 검색 결과 확인

7. 리믹스 제안 페이지 (V1.5)
   ↓ Claude가 생성한 변형 프롬프트 후보 3개 확인
   [copy prompt] → 4번(프롬프트 페어 로그 페이지)으로 루프
```

---

## 기능 명세

### 1. V1 코어 기능 (D13 출시 필수)

| ID | 기능명 | 설명 | MVP 필수 이유 | 관련 페이지 |
|----|--------|------|--------------|------------|
| **F001** | URL/이미지 드롭 + Vision 토큰 분해 | IG/Pinterest/YouTube/Are.na URL 또는 이미지 파일 드롭 시 Claude Sonnet Vision이 subject/style/lighting/composition/medium/mood 6차원 구조화 토큰으로 자동 분해 | "이 레퍼런스 같은 결과를 MJ에서 어떻게 뽑는가"의 역공학 반복이 2시간 병목의 근원 — 언어 번역 gap을 Vision이 메움 | 레퍼런스 라이브러리 페이지 |
| **F002** | 좋아요·태그·프롬프트 스니펫 필드 | 레퍼런스에 1~10 점수, 수동 태그(카테고리/무드/색감/용도), 프롬프트 스니펫 저장 및 [copy prompt] 버튼으로 클립보드 복사 | 취향 데이터 축적 없이는 반복 실험이 의미 없음. copy prompt → MJ 붙여넣기가 실질 워크플로우 단축 핵심 | 레퍼런스 라이브러리 페이지, 레퍼런스 상세 페이지 |
| **F003** | 프롬프트 페어 로그 | MJ에서 실행한 프롬프트 문구 + 결과 이미지를 pair로 저장. "만족 결과" 마킹(체크/별점). 한 프롬프트에 여러 결과 이미지(MJ variations) 연결 가능 | 반복 이터레이션 기록 없이는 무엇이 효과 있었는지 추적 불가. 성공 페어 축적이 V1.5 벡터 검색의 토대 | 프롬프트 페어 로그 페이지 |

### 2. V1 필수 지원 기능

| ID | 기능명 | 설명 | MVP 필수 이유 | 관련 페이지 |
|----|--------|------|--------------|------------|
| **F010** | 기본 인증 | Supabase Auth 이메일 로그인/로그아웃. 단일 계정 전용 | 서비스 이용 최소 인증. 단일 사용자이므로 회원가입 플로우 불필요 (계정 사전 생성) | 로그인 페이지 |

### 3. V1.5 기능 (Week 3 추가, non-blocking)

| ID | 기능명 | 설명 | 추가 이유 | 관련 페이지 |
|----|--------|------|----------|------------|
| **F004** | 토큰 diff UI | 성공/실패 프롬프트 간 토큰 차이 시각화. 시간 순 나열 + 차이 단어 하이라이트 | "muted 추가하니 결과 확 달라짐" 같은 패턴 추적으로 프롬프트 언어 학습 가속 | 토큰 diff 페이지 |
| **F005** | pgvector 유사 레퍼런스 검색 | reference_tokens 임베딩(Voyage-3 1024dim) 기반 유사도 검색. 태그 필터 + 벡터 검색 조합. 새 레퍼런스 드롭 시 유사 성공 프롬프트 N개 자동 제안 | 레퍼런스 축적이 100개 이상 쌓인 후 검색 가치 발생. V1에서 데이터 토대 구축 필요 | 유사 레퍼런스 검색 페이지 |
| **F006** | 리믹스 제안 | 저장된 레퍼런스 선택 후 "이 느낌 × 새 주제" 입력 시 Claude가 프롬프트 변형 후보 3개 생성. 대화형 UX, 폼 최소화 | 프롬프트 언어 표현 속도를 Claude가 보조. 안나의 평가 속도가 표현 속도를 앞지르는 gap을 메움 | 리믹스 제안 페이지 |

### 4. MVP 이후 기능 (명시적 제외)

- 이미지·영상 직접 생성 (생성은 Midjourney·Nano Banana 수동 사용)
- Midjourney API 호출 자동화
- Taste Graph 추천 엔진 (데이터 축적 후 Phase 2)
- 자동 자막 생성 (캡컷 기본 기능 사용)
- 성과 측정 대시보드 (플랫폼 네이티브 인사이트 사용)
- A/B 실험 플랫폼
- 웹서핑 레퍼런스 에이전트 (M5 자동 모니터링)
- Pinterest API 연동
- 훅 생성기·스크립트 공장·투표 UI

---

## 메뉴 구조

```
Prompt Studio v0.5 내비게이션

[상단 헤더 - 로그인 후 상시 노출]
├── 레퍼런스 라이브러리 - F001, F002 (레퍼런스 드롭·태그·스니펫)
├── 프롬프트 페어 로그 - F003 (프롬프트 + 결과 이미지 pair 기록)
├── 토큰 diff - F004 [V1.5] (성공/실패 토큰 차이 시각화)
├── 유사 검색 - F005 [V1.5] (pgvector 유사 레퍼런스 검색)
├── 리믹스 제안 - F006 [V1.5] (Claude 대화형 프롬프트 변형)
└── 로그아웃 - F010

[비로그인 상태]
└── 로그인 - F010
```

---

## 페이지별 상세 기능

> **5-state UX 원칙** (DESIGN-3 autoplan 반영 — 모든 페이지에 공통 적용):
> 각 페이지는 **idle / loading / empty / error / success** 5가지 상태 UI를 구현 시 모두 고려해야 한다. 아래 주요 상태를 간단히 명시하고, 구현 시 누락된 상태는 ROADMAP의 해당 Task에서 세부 처리.
>
> | 페이지 | loading | empty | error | partial |
> |---|---|---|---|---|
> | 로그인 | 버튼 스피너 | N/A | 인라인 에러 메시지 | N/A |
> | 레퍼런스 라이브러리 | skeleton 카드 | 온보딩 카드(URL 예시) | Vision 실패 시 수동 태그 배너 | Vision 대기 중 이미지만 표시 |
> | 레퍼런스 상세 | 상세 skeleton | N/A (진입 전제) | "레퍼런스를 찾을 수 없습니다" | 토큰 편집 중 자동 저장 인디케이터 |
> | 프롬프트 페어 로그 | 페어 저장 중 스피너 | "첫 페어를 저장해보세요" 힌트 + Cmd+V 안내 | 업로드 실패 재시도 버튼 | 일부 이미지만 업로드 성공 시 재시도 |
> | 토큰 diff (V1.5) | diff 계산 중 스피너 | "비교할 페어 2개를 선택하세요" | N/A | N/A |
> | 유사 검색 (V1.5) | 검색 중 skeleton | "0 결과. 다른 키워드 시도" | 임베딩 없는 레퍼런스 알림 | — |
> | 리믹스 제안 (V1.5) | Claude 응답 중 | "기준 레퍼런스 선택 안내" | API 실패 재시도 | 3개 중 일부만 생성된 경우 |

### 로그인 페이지

> **구현 기능:** `F010` | **인증:** 불필요 (인증 전 유일 접근 가능 페이지)

| 항목 | 내용 |
|------|------|
| **역할** | Supabase Auth 이메일 로그인 진입점. 단일 계정 전용이므로 회원가입 UI 없음 |
| **진입 경로** | 앱 최초 접근 시 자동 리디렉션, 또는 로그아웃 후 자동 이동 |
| **사용자 행동** | 이메일·비밀번호 입력 후 로그인 버튼 클릭 |
| **주요 기능** | - 이메일·비밀번호 입력 폼<br>- Supabase Auth 인증 처리<br>- 인증 실패 시 에러 메시지 표시<br>- **로그인** 버튼 |
| **다음 이동** | 성공 → 레퍼런스 라이브러리 페이지 자동 이동, 실패 → 에러 메시지 인라인 표시 |

---

### 레퍼런스 라이브러리 페이지

> **구현 기능:** `F001`, `F002` | **메뉴 위치:** 헤더 최상단, 앱의 메인 허브

| 항목 | 내용 |
|------|------|
| **역할** | URL/이미지 드롭으로 레퍼런스를 수집하고, Vision이 6차원 토큰으로 분해하며, 태그·점수·프롬프트 스니펫을 축적하는 메인 작업 공간 |
| **진입 경로** | 로그인 성공 후 자동 이동, 헤더 "레퍼런스 라이브러리" 클릭 |
| **사용자 행동** | URL 붙여넣기 또는 이미지 파일 드래그 드롭 → Vision 토큰 분해 확인 → 점수/태그/스니펫 입력 → [copy prompt]로 MJ에 붙여넣기 |
| **주요 기능** | - IG/Pinterest/YouTube/Are.na URL 입력 + 이미지 drag-drop 영역<br>- Claude Sonnet Vision 6차원 토큰 자동 분해 표시 (subject/style/lighting/composition/medium/mood)<br>- Vision 호출 실패 시 수동 태그 입력 UI 폴백<br>- 레퍼런스 카드 그리드 (썸네일·토큰·점수·태그 요약)<br>- 1~10 점수 입력, 카테고리/무드/색감/용도 태그 추가<br>- 프롬프트 스니펫 텍스트 필드 (여러 버전 저장 가능)<br>- **[copy prompt]** 버튼 (클립보드 복사)<br>- 레퍼런스 카드 클릭 시 레퍼런스 상세 페이지 이동 |
| **다음 이동** | 레퍼런스 카드 클릭 → 레퍼런스 상세 페이지, [copy prompt] → 클립보드 복사 완료 토스트 |

---

### 레퍼런스 상세 페이지

> **구현 기능:** `F002` | **메뉴 위치:** 레퍼런스 라이브러리 페이지 카드 클릭으로 진입

| 항목 | 내용 |
|------|------|
| **역할** | 개별 레퍼런스의 토큰 분해 결과, 점수, 태그, 프롬프트 스니펫 전체를 확인하고 편집하는 상세 뷰 |
| **진입 경로** | 레퍼런스 라이브러리 페이지의 레퍼런스 카드 클릭 |
| **사용자 행동** | 토큰 분해 결과 확인·수정, 태그 추가·삭제, 프롬프트 스니펫 편집, [copy prompt] 실행 |
| **주요 기능** | - 원본 이미지/썸네일 전체 크기 표시<br>- 6차원 토큰 값 확인 및 수동 편집<br>- 점수(1~10) 및 태그 편집<br>- 프롬프트 스니펫 목록 (버전별 나열, 추가·삭제)<br>- **[copy prompt]** 버튼 (스니펫별)<br>- 레퍼런스 삭제 |
| **다음 이동** | 뒤로가기 → 레퍼런스 라이브러리 페이지 |

---

### 프롬프트 페어 로그 페이지

> **구현 기능:** `F003` | **메뉴 위치:** 헤더 "프롬프트 페어 로그"

| 항목 | 내용 |
|------|------|
| **역할** | MJ에서 실제 실행한 프롬프트와 결과 이미지를 pair로 기록하고, 만족 결과를 마킹해 성공 페어 라이브러리를 축적하는 페이지 |
| **진입 경로** | 헤더 "프롬프트 페어 로그" 클릭, 또는 레퍼런스 상세 페이지에서 [copy prompt] 후 이동 |
| **사용자 행동** | 프롬프트 문구 입력 → 결과 이미지 업로드 → 만족도 마킹 → pair 저장. 기존 페어 목록에서 만족 결과 마킹 또는 수정 |
| **주요 기능** | - 프롬프트 텍스트 입력 필드 (MJ에서 사용한 프롬프트 그대로 붙여넣기)<br>- 결과 이미지 업로드 (drag-drop, 한 프롬프트에 여러 이미지 연결 가능)<br>- 만족도 마킹 (체크 또는 1~5 별점)<br>- is_final_pick 체크박스 (최종 채택 여부)<br>- 세션별 이터레이션 누적 카운트 자동 기록<br>- 저장된 페어 목록 (시간 역순, 만족 결과 필터)<br>- **[페어 저장]** 버튼 |
| **다음 이동** | 저장 완료 → 목록 갱신. 페어 클릭 → 토큰 diff 페이지 (V1.5 연결) |

---

### 토큰 diff 페이지 (V1.5)

> **구현 기능:** `F004` | **메뉴 위치:** 헤더 "토큰 diff"

| 항목 | 내용 |
|------|------|
| **역할** | 성공 프롬프트와 실패 프롬프트 간 토큰 차이를 시각화해 "어떤 단어를 추가/제거했을 때 결과가 바뀌었는가"를 추적하는 분석 뷰 |
| **진입 경로** | 헤더 "토큰 diff" 클릭, 또는 프롬프트 페어 로그 페이지에서 페어 클릭 |
| **사용자 행동** | 비교할 프롬프트 2개 선택 → 차이 단어 하이라이트 확인 → 패턴 학습 |
| **주요 기능** | - 시간 순 페어 목록에서 비교 대상 2개 선택<br>- 프롬프트 텍스트 나란히 표시 (추가 단어 초록, 제거 단어 빨강 하이라이트)<br>- 만족/불만족 마킹 여부 표시<br>- **[비교 보기]** 버튼 |
| **다음 이동** | 결과 확인 후 레퍼런스 라이브러리 또는 프롬프트 페어 로그 페이지로 이동 |

---

### 유사 레퍼런스 검색 페이지 (V1.5)

> **구현 기능:** `F005` | **메뉴 위치:** 헤더 "유사 검색"

| 항목 | 내용 |
|------|------|
| **역할** | pgvector 임베딩 기반으로 취향/스타일이 유사한 레퍼런스와 그에 연결된 성공 프롬프트를 검색해 새 작업의 프롬프트 출발점을 제공하는 검색 허브 |
| **진입 경로** | 헤더 "유사 검색" 클릭, 또는 새 레퍼런스 드롭 시 자동 유사도 검색 트리거 |
| **사용자 행동** | 검색어 또는 태그 필터 입력 → 유사 레퍼런스 목록 확인 → 연결된 성공 프롬프트 확인 → [copy prompt] 실행 |
| **주요 기능** | - 텍스트 검색 입력 (6차원 토큰 기반 임베딩 유사도 검색)<br>- 태그 필터 (카테고리/무드/색감/용도 다중 선택)<br>- 유사 레퍼런스 목록 (썸네일 + 유사도 스코어)<br>- 각 레퍼런스에 연결된 성공 프롬프트 스니펫 표시<br>- **[copy prompt]** 버튼 |
| **다음 이동** | 레퍼런스 클릭 → 레퍼런스 상세 페이지, [copy prompt] → 클립보드 복사 |

---

### 리믹스 제안 페이지 (V1.5)

> **구현 기능:** `F006` | **메뉴 위치:** 헤더 "리믹스 제안"

| 항목 | 내용 |
|------|------|
| **역할** | 저장된 레퍼런스의 스타일/감도를 새 주제에 적용한 프롬프트 변형 후보 3개를 Claude가 대화형으로 생성하는 창작 보조 페이지 |
| **진입 경로** | 헤더 "리믹스 제안" 클릭, 또는 레퍼런스 상세 페이지의 "리믹스" 버튼 클릭 |
| **사용자 행동** | 기준 레퍼런스 선택 → 새 주제/주요 변경 사항 입력 → Claude 후보 3개 확인 → 마음에 드는 후보 [copy prompt] → MJ 실행 → 결과를 프롬프트 페어 로그 페이지에 저장 |
| **주요 기능** | - 기준 레퍼런스 선택 (라이브러리에서 검색·선택)<br>- "이 느낌 × 새 주제" 자연어 입력 필드 (폼 최소화, 대화형 UX)<br>- Claude Sonnet이 생성한 변형 프롬프트 후보 3개 카드<br>- 각 후보별 **[copy prompt]** 버튼<br>- 후보 재생성 버튼 |
| **다음 이동** | [copy prompt] → 클립보드 복사 후 프롬프트 페어 로그 페이지로 이동 유도 |

---

## 데이터 모델

### references (레퍼런스 원본)

| 필드 | 설명 | 타입/관계 |
|------|------|----------|
| id | 고유 식별자 | UUID |
| source_url | IG/Pinterest/YouTube/Are.na 원본 URL | text |
| thumbnail_url | 썸네일 이미지 URL (스크래핑 또는 업로드) | text |
| favorite_score | 좋아요 점수 (1~10) | int |
| notes | 메모 | text |
| user_id | 사용자 식별자 | → auth.users.id |
| uploaded_at | 등록 일시 | timestamp |

### reference_tokens (Vision 토큰 분해 결과)

| 필드 | 설명 | 타입/관계 |
|------|------|----------|
| id | 고유 식별자 | UUID |
| reference_id | 레퍼런스 참조 | → references.id |
| tokens | 6차원 구조화 토큰 JSONB (subject/style/lighting/composition/medium/mood) | jsonb |
| embedding | Voyage-3 1024차원 벡터 임베딩 | vector(1024) |
| vision_model_version | Vision 호출 모델 버전 | text |
| created_at | 생성 일시 | timestamp |

### tags (수동 태그)

| 필드 | 설명 | 타입/관계 |
|------|------|----------|
| id | 고유 식별자 | UUID |
| reference_id | 레퍼런스 참조 | → references.id |
| tag | 태그 텍스트 | text |
| tag_kind | 태그 유형 (category/mood/color/purpose) | enum |

### prompts (프롬프트 스니펫)

| 필드 | 설명 | 타입/관계 |
|------|------|----------|
| id | 고유 식별자 | UUID |
| reference_id | 연결 레퍼런스 (nullable — pair에서 직접 입력 시) | → references.id |
| prompt_text | 프롬프트 전체 문구 | text |
| source | 출처 구분 (copied/modified/remix) | enum |
| created_at | 생성 일시 | timestamp |

### pairs (프롬프트 페어 로그)

| 필드 | 설명 | 타입/관계 |
|------|------|----------|
| id | 고유 식별자 | UUID |
| prompt_id | 연결 프롬프트 | → prompts.id |
| result_image_url | MJ 결과 이미지 URL | text |
| satisfaction | 만족도 (null/1~5) | int |
| is_final_pick | 최종 채택 여부 | bool |
| iteration_count_cumulative | 세션 내 누적 이터레이션 수 | int |
| session_id | 작업 세션 구분자 | text |
| created_at | 생성 일시 | timestamp |

---

## 기술 스택 (최신 버전)

### 프론트엔드 프레임워크

- **Next.js 15** (App Router) — React 풀스택 프레임워크. 서버 컴포넌트로 Vision API 호출 처리
- **TypeScript 5.6+** — 타입 안전성 보장. any 타입 사용 금지
- **React 19** — UI 라이브러리 (최신 동시성 기능 활용)

### 스타일링 & UI

- **TailwindCSS v3.4** (Supabase Next.js 공식 스타터킷 기본 구성, `tailwind.config.ts` + `@tailwind` directives + HSL CSS 변수) — 유틸리티 CSS 프레임워크
- **shadcn/ui** (style: new-york, baseColor: neutral, cssVariables: true) — 고품질 React 컴포넌트 라이브러리 (드래그 드롭, 카드, 배지 등)
- **tailwindcss-animate** — shadcn/ui 애니메이션 플러그인 (스타터킷 기본 포함)
- **Lucide React** — 아이콘 라이브러리

### 폼 & 검증

- **React Hook Form 7.x** — 폼 상태 관리 (프롬프트 스니펫 입력, 태그 입력)
- **Zod** — 스키마 검증 (6차원 토큰 JSONB 구조 검증)

### AI & 임베딩

- **Claude Sonnet Vision (Anthropic API)** — 이미지 6차원 구조화 토큰 분해 (tool_use / JSON mode로 스키마 드리프트 방지)
- **Voyage-3 (1024 dim)** — 구조화 토큰 텍스트 임베딩 (pgvector 유사도 검색용)

### 백엔드 & 데이터베이스

- **Supabase** — BaaS (Auth 단일 계정, PostgreSQL, Storage, pgvector 확장)
- **PostgreSQL + pgvector** — 관계형 DB + 벡터 유사도 검색 (reference_tokens.embedding)

### 배포 & 호스팅

- **Vercel** — Next.js 15 최적화 배포. git push → 자동 preview 배포

### 패키지 관리

- **npm** — 의존성 관리
